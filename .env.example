# SeekTalent 0.7.1 local runtime requires one user-provided secret.
# Defaults use Bailian for LLM calls and Liepin through the local OpenCLI browser bridge.
# Optional advanced overrides still exist in AppSettings; set them only when needed.
SEEKTALENT_TEXT_LLM_API_KEY=
